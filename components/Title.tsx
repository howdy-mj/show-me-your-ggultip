import React from 'react';
import styled from 'styled-components'

const Title = () => {
  return (
    <TempoTitle>
      아무개의 꿀팁저장소
    </TempoTitle>
  );
};

export default Title;

const TempoTitle = styled.h1`
  color: cadetblue;
`
